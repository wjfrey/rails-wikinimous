<div class="container">
  <div class="article-card">
    <h4><%= @article.title %></h4>
    <p><%= @article.content %></p>
    <br>
    <h4><%= link_to "Edit Article", edit_article_path(@article), class: 'btn btn-primary' %></h4>
  </div>
  <%= link_to "Back to Overview", articles_path, class: 'btn btn-inferior'%>
</div>
