<h1>Wikinimous</h1>

<ul>
  <% @articles.each do |article| %>
    <li> <%= link_to article.title, article_path(article) %> <%= link_to 'Delete Article', article_path(article), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'%></li>
  <% end %>
</ul>


<h4><%= link_to "Create New Article", new_article_path %></h4>
